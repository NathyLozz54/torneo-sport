spring:
  r2dbc:
    url: r2dbc:postgresql://localhost:5432/torneo_sport
    username: postgresuser
    password: postgrespass
  flyway:
    enabled: false
    url: jdbc:postgresql://localhost:5432/torneo_sport
    user: postgresuser
    password: postgrespass
    locations: classpath:db/migration
  sql:
    init:
      mode: always

logging:
  level:
    root: INFO
    org.springframework.r2dbc.connection: DEBUG
